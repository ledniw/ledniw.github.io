'use strict';

describe('myApp.disability module', function() {

  beforeEach(module('myApp.disability'));

  describe('disability controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var disabilityCtrl = $controller('DisabilityCtrl');
      expect(disabilityCtrl).toBeDefined();
    }));

  });
});