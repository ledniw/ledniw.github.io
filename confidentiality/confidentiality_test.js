'use strict';

describe('myApp.confidentiality module', function() {

  beforeEach(module('myApp.confidentiality'));

  describe('confidentiality controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var confidentialityCtrl = $controller('ConfidentialityCtrl');
      expect(confidentialityCtrl).toBeDefined();
    }));

  });
});