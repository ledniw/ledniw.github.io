'use strict';

describe('myApp.credentials module', function() {

  beforeEach(module('myApp.credentials'));

  describe('credentials controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var credentialsCtrl = $controller('CredentialsCtrl');
      expect(credentialsCtrl).toBeDefined();
    }));

  });
});