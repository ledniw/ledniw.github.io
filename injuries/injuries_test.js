'use strict';

describe('myApp.injuries module', function() {

  beforeEach(module('myApp.injuries'));

  describe('injuries controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var injuriesCtrl = $controller('InjuriesCtrl');
      expect(injuriesCtrl).toBeDefined();
    }));

  });
});