'use strict';

describe('myApp.homeassesment module', function() {

  beforeEach(module('myApp.homeassesment'));

  describe('homeassesment controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeassesmentCtrl = $controller('HomeassesmentCtrl');
      expect(homeassesmentCtrl).toBeDefined();
    }));

  });
});